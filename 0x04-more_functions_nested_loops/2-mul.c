#include "main.h"
/**
 * mul - functs for checks for a digit (0 success).
 * @a: a - Variable
 * @b: b - variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
